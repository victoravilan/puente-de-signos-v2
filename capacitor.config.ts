import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vmac.puentedesignos.v2',
  appName: 'Puente de Signos',
  webDir: 'dist'
};

export default config;
